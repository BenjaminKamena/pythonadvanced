import tkinter as tk
import pandas as pd
from time import strftime
#from flight_scraper_bot import Flight_Bot

window = tk
window.title("FLIGHT SCRAPER")
window.resizable(width="false", height="false")
frame_header = tk.Frame(window, borderwidth=2, pady=2)
center_frame = tk.Frame(window, borderwidth=2, pady=2)
bottom_frame = tk.Frame(window, borderwidth=2, pady=2)

window.mainloop()